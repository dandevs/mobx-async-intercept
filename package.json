{
  "name": "mobx-async-intercept",
  "description": "Use mobx intercepts with promises/async",
  "main": "dist/index.js",
  "version": "1.1.7",
  "license": "ISC",
  "scripts": {
    "build": "tsc --p tsconfig.prod.json",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@types/jest": "^23.3.2",
    "jest": "^23.6.0",
    "then-sleep": "^1.0.1",
    "typescript": "^3.0.3"
  },
  "peerDependencies": {
    "mobx": "^5.1.1"
  },
  "keywords": [
    "mobx",
    "async",
    "intercept",
    "promise"
  ],
  "homepage": "https://github.com/dandevs/mobx-async-intercept",
  "jest": {
    "testEnvironment": "node"
  }
}
